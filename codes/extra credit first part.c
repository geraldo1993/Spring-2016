#include <stdio.h>

int main (void)
{
	int a[10];
	int i;
	int largest;
	
	printf("Enter wanted integer %d: \n", i+1);
	scanf("%d",&a[i]);
	
  for(i=0;i<10;i++)
   {
     
    {
          if(largest<a[i])
            largest=a[i];
    }
  printf("Largest= %d\n",largest);
	
    }
	return 0;
}
