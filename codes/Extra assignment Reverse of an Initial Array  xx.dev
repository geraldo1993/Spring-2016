#include <stdio.h>
int main (void)
{
 int a[10];
 printf("Enter the elements of the array:\n", a[10]);
 

int aReverse[10]; 
int i; 
int digit; 
while(i !=0) 
{ 
digit=i%10; //modulo 
reverse=reverse*10+digit; 
i=i/10; 
} 
printf("reverse number=%d ",reverse);
	return 0;
	
}
